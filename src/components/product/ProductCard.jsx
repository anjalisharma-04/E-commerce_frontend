import { Link } from 'react-router-dom'; import { useCart } from '../../services/CartContext'; const ProductCard = ({ id, title, price }) => { const { addToCart } = useCart(); return ( <div className='border rounded-lg p-4 hover:shadow-lg transition hover:-translate-y-1'> <Link to={/products/}> <div className='h-32 bg-gray-100 rounded mb-3 flex items-center justify-center'>Image</div> <h3 className='font-semibold mb-1'>{title}</h3> <p className='text-orange-500 font-bold'>?{price}</p> </Link> <button onClick={() => addToCart({ id, title, price })} className='mt-3 w-full bg-orange-500 text-white py-2 rounded hover:bg-orange-600'> Add to Cart </button> </div> ); }; export default ProductCard;
